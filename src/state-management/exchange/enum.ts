export enum CurrencyType {
  USD = 'USD',
  EUR = 'EUR',
  GBP = 'GBP',
  RON = 'RON',
  CAD = 'CAD',
}

export enum OpenCurrencyListType {
  From,
  To,
}

export enum ExchangeType {
  Sell = 'Sell',
  Buy = 'Buy',
}
